import { generateSecureUniqueId } from './generateUniqueId.js';
export const pushRequesterSecret = async (iexec, secrets) => {
    const secretsId = {};
    for (const key in secrets) {
        if (secrets.hasOwnProperty(key)) {
            const secret = secrets[key];
            const requesterSecretId = generateSecureUniqueId(16);
            await iexec.secrets.pushRequesterSecret(requesterSecretId, secret);
            secretsId[key] = requesterSecretId;
        }
    }
    return secretsId;
};
//# sourceMappingURL=pushRequesterSecret.js.map