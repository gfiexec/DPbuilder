export const formatGrantedAccess = (order) => Object.fromEntries(Object.entries(order).map(([key, val]) => [
    key,
    val.toString().toLowerCase(),
]) // stringify numbers and lowercase addresses to return a clean GrantedAccess
);
//# sourceMappingURL=formatGrantedAccess.js.map