import { zeroPadValue } from 'ethers';
import { GROUP_MEMBER_PURPOSE } from '../../../config/config.js';
export const isAddressInWhitelist = async ({ whitelistContract, address, }) => {
    return whitelistContract.keyHasPurpose(zeroPadValue(address, 32), GROUP_MEMBER_PURPOSE);
};
//# sourceMappingURL=whitelistContract.read.js.map