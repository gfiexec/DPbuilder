import { Contract } from 'ethers';
import { ABI } from '../../../../generated/abis/core/interfaces/IDataProtector.sol/IDataProtector.js';
export async function getDataProtectorCoreContract(iexec, contractAddress) {
    const { signer } = await iexec.config.resolveContractsClient();
    return new Contract(contractAddress, ABI).connect(signer);
}
//# sourceMappingURL=getDataProtectorCoreContract.js.map