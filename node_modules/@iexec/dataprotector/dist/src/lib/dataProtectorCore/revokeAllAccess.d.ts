import { RevokeAllAccessParams, RevokedAccess } from '../types/coreTypes.js';
import { IExecConsumer } from '../types/internalTypes.js';
export declare const revokeAllAccess: ({ iexec, protectedData, authorizedApp, authorizedUser, onStatusUpdate, }: IExecConsumer & RevokeAllAccessParams) => Promise<RevokedAccess[]>;
