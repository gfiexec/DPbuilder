import { GrantAccessParams, GrantedAccess } from '../types/index.js';
import { IExecConsumer } from '../types/internalTypes.js';
export declare const grantAccess: ({ iexec, protectedData, authorizedApp, authorizedUser, pricePerAccess, numberOfAccess, onStatusUpdate, }: IExecConsumer & GrantAccessParams) => Promise<GrantedAccess>;
