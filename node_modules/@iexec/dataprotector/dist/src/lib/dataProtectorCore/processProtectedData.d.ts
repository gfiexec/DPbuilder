import { ProcessProtectedDataParams, ProcessProtectedDataResponse } from '../types/index.js';
import { IExecConsumer } from '../types/internalTypes.js';
export type ProcessProtectedData = typeof processProtectedData;
export declare const processProtectedData: ({ iexec, protectedData, app, userWhitelist, maxPrice, args, inputFiles, secrets, workerpool, onStatusUpdate, }: IExecConsumer & ProcessProtectedDataParams) => Promise<ProcessProtectedDataResponse>;
