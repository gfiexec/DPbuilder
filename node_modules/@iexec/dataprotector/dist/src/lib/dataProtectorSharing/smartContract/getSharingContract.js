import { Contract } from 'ethers';
import { ABI } from '../../../../generated/abis/sharing/DataProtectorSharing.sol/DataProtectorSharing.js';
export async function getSharingContract(iexec, sharingContractAddress) {
    const { signer } = await iexec.config.resolveContractsClient();
    return new Contract(sharingContractAddress, ABI).connect(signer);
}
//# sourceMappingURL=getSharingContract.js.map