import { Contract } from 'ethers';
import { ABI } from '../../../../generated/abis/sharing/registry/AddOnlyAppWhitelist.sol/AddOnlyAppWhitelist.js';
export async function getAppWhitelistContract(iexec, addOnlyAppWhitelist) {
    const { signer } = await iexec.config.resolveContractsClient();
    return new Contract(addOnlyAppWhitelist, ABI).connect(signer);
}
//# sourceMappingURL=getAddOnlyAppWhitelistContract.js.map